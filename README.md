## 📡 컴퓨터 네트워크 패킷 분석 프로젝트 (Computer Network Project)

본 프로젝트는 컴퓨터 네트워크 과목의 설계 과제로, Raw Socket(Python)과 Wireshark를 활용하여 네트워크 패킷을 캡처하고 분석하는 과정을 구현한 것입니다.
<br><br>
### 📁 프로젝트 구성

backend/: 서버 및 클라이언트 구현을 위한 Python 코드 모음

dns_server.py: UDP 기반 로컬 DNS 서버 (Raw Socket 활용)

web_server.py: TCP 기반 HTTP 웹 서버 (Flask 활용)

http_sniffer.py: TCP 패킷을 감지하는 Raw Socket 스니퍼

templates/: 웹 서버가 제공하는 HTML 파일들

captures/: Wireshark 캡처 파일 (.pcap)
<br><br>
### 🛠️ 실행 환경 준비 (필수)

본 프로젝트는 Python 3 환경에서 동작하며, 일부 기능은 관리자 권한(sudo)이 필요합니다.

1. Python 및 라이브러리 설치

<br>

**Flask (웹 서버용) 및 Scapy (패킷 생성/분석용) 설치**
```
# 윈도우용
pip install flask scapy

# 맥북용
pip3 install flask scapy
```

<br>

### 🚀 실행 가이드

원활한 테스트를 위해 3개의 터미널을 열고 아래 순서대로 실행하는 것을 권장합니다.

<br>

1. DNS 서버 실행 (터미널 1)

역할: network.local 도메인에 대한 서버 IP 주소(서버를 실행시킬 IP 주소)를 응답합니다.

포트: UDP 53번 (관리자 권한 필요)

```
# 맥북 기준
cd backend
sudo python3 dns_server.py
```

<br>

2. 웹 서버 실행 (터미널 2)

역할: 웹 페이지(HTML)를 제공하고 리다이렉트 응답을 보냅니다.

포트: TCP 80번

```
# 맥북 기준
cd backend
sudo python3 web_server.py
```

<br>

3. HTTP 스니퍼 실행 (터미널 3 - 선택 사항)

역할: 서버로 들어오는 HTTP 패킷 내용을 실시간으로 감지하여 출력합니다.

주의: 코드 내 iface 설정을 확인하세요 (en0: 와이파이, lo0: 로컬).

```
# 맥북 기준
cd backend
sudo python3 http_sniffer.py
```

<br>

### 🧪 테스트 방법 (클라이언트)

다른 컴퓨터(클라이언트)는 서버와 같은 와이파이에 연결한 후 진행합니다.

DNS 조회 테스트 (CMD/터미널):

```
# [서버 IP] 부분에 서버 컴퓨터의 실제 IP를 입력하세요.
nslookup network.local [서버 IP]
```

<br>

### 📊 분석 도구 (Wireshark)

실행 위치: 서버 컴퓨터

인터페이스: Wi-Fi (en0)

필터:
```
(udp.port == 53 || tcp.port == 80 || tcp.port == 5500) && ip.addr == [클라이언트 IP]
```
